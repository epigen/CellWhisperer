Figures pipeline
======================

This pipeline generates all plots (where possible) for our manuscript.

Note: Some of the panels are screenshots that were directly captured from the web-app.

Also, the plot data is usually generated in the respective pipelines (e.g. `llava`)
