method: random
name: LU_sweep
metric:
  goal: minimize
  name: val_loss
parameters:
  model.learning_rate:
    distribution: log_uniform_values
    min: 1e-5
    max: 1e-2
  model.frozen_warmup:
    values:
      - 0.0
      - 0.03
