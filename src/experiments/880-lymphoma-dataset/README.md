The way to go:

- Use `extract_initial_mask.py` to produce two pngs.
- Open them in inkscape, then determine the best rotation and scaling
- Introduce them into main.py
- then run main.py to find the best location and spit out the equations
